# Contribution Guidelines

1. Always branch from `dev`: `git checkout -b feat/mX-feature-name`
2. Keep changes within your own component/controller files.
3. Open a pull request (PR) into `dev` (not `main`).
4. Ensure the PR checklist is complete.
5. Request review from the CODEOWNER of the files you touched.
